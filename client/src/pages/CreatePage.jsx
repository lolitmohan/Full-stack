import React from 'react';
import CreateForm from '../Component/CreateForm';

const CreatePage = () => {
    return (
        <div>
            <CreateForm/>
        </div>
    );
};

export default CreatePage;